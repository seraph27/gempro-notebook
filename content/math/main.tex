\subsection{Algoritmo Euclideano}
Encontra inteiros $x$, $y$ e $g$ tais que $ax + by = g$ e $g = gcd(a, b)$. Útil para calcular inversos módulo $M$.
Complexidade: $O(\log(\min(a, b)))$.
\addtemplate{math/egcd.cpp}
\subsection{CRT}
